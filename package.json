{
  "name": "muzo-lyrics-canvas-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for muzo lyrics and canvas with TiDB serverless database caching",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@tidbcloud/serverless": "latest"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
